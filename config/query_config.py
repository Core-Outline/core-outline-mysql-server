import operator
queries_table = 'queries'

query_actions = {
    'less_than': '<',
    'greater_than': '>',
    # 'in': operator.,
    'greater_than_equal_to': '>=',
    'less_than_equal_to': '<=',
}
